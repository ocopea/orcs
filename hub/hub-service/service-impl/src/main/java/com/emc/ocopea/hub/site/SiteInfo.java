// Copyright (c) [2017] Dell Inc. or its subsidiaries. All Rights Reserved.
package com.emc.ocopea.hub.site;

/**
 * Created by liebea on 11/29/15.
 * Drink responsibly
 */
public class SiteInfo {
/*
    private final Map<String, SupportedAppTemplateInfo> supportedAppTemplates = new HashMap<>();
    private final Map<String, Map<String, SupportedService>> supportedServices = new HashMap<>();
    private final Map<String, Map<String, AppInstanceInfo>> appInstances = new HashMap<>();

    public Map<String, SupportedAppTemplateInfo> getSupportedAppTemplates() {
        return supportedAppTemplates;
    }

    public Map<String, Map<String, SupportedService>> getSupportedServices() {
        return supportedServices;
    }

    public void addSupportedAppTemplates(Collection<SupportedAppTemplateInfo> appTemplates){
        for (SupportedAppTemplateInfo curr : appTemplates){
            supportedAppTemplates.put(curr.getName(), curr);
        }
    }

    public void addAppInstances(Collection<AppInstanceInfo> appInstanceInfos){
        for (AppInstanceInfo curr : appInstanceInfos){
            Map<String, AppInstanceInfo> byAppName = appInstances.get(curr.getAppInstanceId());
            if (byAppName == null){
                byAppName = new HashMap<>();
                appInstances.put(curr.getAppInstanceId(), byAppName);
            }
            byAppName.put(curr.getRootURL(), curr);
        }
    }
*/
}
